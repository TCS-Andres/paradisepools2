// Placeholder. A downstream worker owns the QuoteForm content.
// Anchor id is "quote": every CTA on the page links here.
export default function QuoteForm() {
  return (
    <section id="quote" className="mx-auto max-w-6xl px-4 py-16">
      {/* TODO: QuoteForm section content. */}
      <p className="font-body text-charcoal/40">QuoteForm placeholder</p>
    </section>
  );
}
