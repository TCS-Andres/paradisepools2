// Placeholder. A downstream worker owns the TrustBar content. No anchor id.
export default function TrustBar() {
  return (
    <section className="mx-auto max-w-6xl px-4 py-8">
      {/* TODO: TrustBar section content. */}
      <p className="font-body text-charcoal/40">TrustBar placeholder</p>
    </section>
  );
}
